<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
  <div class="container">
    <div class="col">
      <div class="row">
        <div class="d-flex justify-content-start">
          <div class="logo py-2 px-0 px-lg-3">
            <%= link_to "LOGO", root_path %>
          </div>
          <button type="button" class="btn btn-primary ml-3 px-0 px-lg-3"><%= link_to "Déposer une annonce", new_product_path(@new_products), class:"text-white" %> </button>
        </div>
        <div class="col-auto d-flex justify-content-end collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav">
            <li class="nav-item active">
              <a class="nav-link" href="#">Mes recherches</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Favoris</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Messages</a>
            </li>
            <li class="nav-item dropdown">
            <% if user_signed_in? %>
              <%= link_to "Déconnecter", destroy_user_session_path, class: "dropdown-item py-2 px-0 px-lg-3 rounded", data: {turbo_method: :delete} %>
            </li>
            <% else %>
            <li class="nav-item">
              <%= link_to "Se connecter", new_user_session_path, class: "nav-link py px-0 px-lg-3 rounded" %>
            </li>
            <% end %>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>
